/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Nếu bạn có dùng hình ảnh từ domain bên ngoài, hãy thêm vào đây
  // images: {
  //   domains: ['example.com'],
  // },
}

module.exports = nextConfig
